# InkFlow AI 2.6 - 右侧抽屉可收起展开、模型弹窗点击空白关闭与导航极简化 Walkthrough

已通过 SenseVoice 本地语音识别引擎精准转录了您的 2 段最新语音要求，并完成了界面的全面精简、Bug 修复与右侧栏抽屉式收起展开升级。

---

## 🎙️ 语音转录与需求对比

1. **语音段落 1**:
   > "第一个呢，就是左边这一栏把聊天这个去掉。技能这一块以后对接 skill 平台。项目列表做公众号、小红书、短视频脚本，新建项目的地方已经有创作类型了，其实非常好，那么就把下面的对话框里面的这个类型给去掉。右边切换 AI 引擎的地方把 Claude 和 OpenAI 去掉，只留 DeepSeek。还有一个 bug 是点击其他地方的时候切换模型这个窗口没有消失，要让它消失掉。右边上传文件这个地方不支持收起来有点占宽度，要让它支持展开收起。"
2. **语音段落 2**:
   > "收起来之后需要保持这个 icon（文件图标），文字不让它展示了。点击这个 icon 又可以展示出来... 你可以想想看怎么做。"

---

## 🎨 界面与功能改动清单

```
┌─────────────────┬──────────────────────────────────┬─────────────────────────────┐
│ 1. 左侧导航栏   │ 2. 中间对话框                    │ 3. 右侧【文件管理】抽屉     │
│  - 移除“聊天”   │  - 移除了输入框处的渠道下拉菜单  │  - 展开状态 (320px):        │
│    全局导航页签 │    (创作类型已在新建项目时确定)  │    展示文件树/搜索/下载/删除│
│  - 保留“技能”   │  - AI 模型下拉框精简:            │  - 收起状态 (56px 窄栏):    │
│    (后续对接)   │    仅保留 DeepSeek V4 Flash &    │    隐藏文字，仅保留 📁 📥   │
│  - 项目列表     │    DeepSeek V4 Pro              │    图标，点击 📁 极速展开/收起│
│    (支持重命名) │  - 🐛 修复 Bug: 增加外部点击监听 │                             │
│                 │    点击任意空白处自动收起模型弹窗  │                             │
└─────────────────┴──────────────────────────────────┴─────────────────────────────┘
```

1. **左侧栏极简导航**：
   - 移除了“聊天”页签，保留 **技能**（后续对接收集 Skill 平台）。
   - 保留 **项目列表**，创建项目时选择项目创作类型（`微信公众号` / `小红书` / `短视频脚本` / `通用交流`）。

2. **中间对话框与 AI 模型选择器 Bug 修复**：
   - **移除输入框渠道下拉框**：项目类型在新建时已确定，输入栏保持极致干练。
   - **模型选择器精简**：仅保留现已上线的 `DeepSeek V4 Flash` (默认) 与 `DeepSeek V4 Pro`，移除了未配置的 OpenAI / Claude 选项。
   - **🐛 修复点击空白关闭 Bug**：新增 `mousedown` 监听，点击下拉框外的任意空白处立刻自动关闭模型选择弹窗。

3. **右侧【文件管理】抽屉式折叠与展开 (`FileManagerPanel.tsx`)**：
   - 右侧栏 Header 新增 `[ ≫ ]` 收起按钮。
   - **收起状态 (56px)**：宽度平滑收缩至 56px 窄栏，隐藏文件名与文字，仅保留 📁 文件夹图标、📥 上传图标与 🔄 刷新图标。
   - 点击 📁 图标或 `[ ≪ ]` 展开按钮，右侧栏平滑扩展恢复至 **320px** 完整列表！

---

## 🧪 验证与测试结果

- **后端 Python 接口校验**: 语法校验 100% 通过。
- **前端 Production Build**: Next.js 14 打包编译 100% 成功，0 错误：
```
✓ Compiled successfully
✓ Generating static pages (4/4)
Route (app)                              Size     First Load JS
┌ ○ /                                    14.4 kB        98.6 kB
```
- **实时运行**: 前端 [http://localhost:3000](http://localhost:3000) 与后端 [http://localhost:8000](http://localhost:8000) 处于后台 Live 运行状态。
